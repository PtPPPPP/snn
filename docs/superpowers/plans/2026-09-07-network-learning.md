# 网络学习实验：替代奖励参数搜索页

用户批准以网络为中心的四阶段设计并要求实现。本计划取代 2026-09-07-rocket-training.md 的参数搜索方案。

- [x] network-learning.ts：3 输入、4 tanh 隐藏单元、3 softmax 策略输出与 1 价值输出，共 36 参数。收集 4 次真实物理飞行，固定旧网络，0.1 秒决策、0.01 秒积分。
- [x] 冻结 Monte Carlo 优势，计算批平均策略损失与价值平方损失；手写反向传播，SGD 0.03。显示真实梯度、预览更新、显式应用、下一批采样。明确非 PPO，无环境反传。
- [x] 测试所有参数的解析梯度与中心差分一致；验证精确更新与输入参数不变。
- [x] 前向、环境、反向、更新四阶段共用网络；点选/键盘/下拉选择连接，经历时间轴相邻，固定输入的新旧输出对照。
- [x] 原参数搜索页面代码与测试移除，保留真实 PPO 推理入口。公式使用高中表达，专业实现展开查看。
- [x] 多端布局、减少动态效果偏好、后台暂停动画、卸载停止；六尺寸交互检查、lint、类型检查和构建。

边界：本机随机初始化教学网络；非原项目训练记录；仅 3 档油门。阶段自动播放不自动提交参数更新；不保证少量训练后成功降落。
